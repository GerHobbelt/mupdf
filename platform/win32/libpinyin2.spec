
# we've got conflicting directories in thirdparty/owemdjee/ : libpinyin & pinyin --> we must make sure the two projects shall never overlap!
only-directories:
	../../thirdparty/owemdjee/pinyin/
	
also-ignore:
	# we've got conflicting directories in thirdparty/owemdjee/ : libpinyin & pinyin --> we must make sure the two projects shall never overlap!
	
		